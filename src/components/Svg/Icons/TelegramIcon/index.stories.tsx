import React from 'react';
import { TelegramIcon } from '.';

export default {
  title: 'Svg/Icons/TelegramIcon',
  component: TelegramIcon,
};

export const Svg = () => <TelegramIcon />;

Svg.story = {
  name: 'default',
};
